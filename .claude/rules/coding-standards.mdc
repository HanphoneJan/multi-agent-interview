---
description: 编码规范与最佳实践。命名、注释、错误处理、提前返回、类型与项目结构（Python/Vue/TS）。
globs: ["**/*.py", "**/*.vue", "**/*.ts", "**/*.js"]
---

# 编码规范与最佳实践

提取自项目采纳的编码规范，适用于 Python 后端与 Vue/TypeScript 前端。

---

## 一、命名规范

### Python

- 类名：大驼峰（如 `UserAuthService`）
- 函数/变量/文件：小写下划线（如 `validate_token`、`user_info`、`rag_chain.py`）
- 常量：全大写下划线（如 `MAX_RETRY_COUNT`）
- 布尔：`is_` / `has_` / `can_` 开头（如 `is_valid`、`has_permission`）

### TypeScript / JavaScript / Vue

- 类/组件/接口：大驼峰（如 `UserAuthService`、`GlobalFilter.vue`）
- 函数/变量：小驼峰（如 `validateToken`、`userInfo`）
- 常量：全大写下划线或小驼峰（如 `API_BASE_URL`）
- 文件名：小写短横线（如 `app-sidebar.vue`）
- 布尔：`is` / `has` / `can` 开头（如 `isLoading`、`hasError`）

### 通用

- 见名知义，避免单字母变量（循环中的 `i`、`j`、`k` 除外）
- 禁止魔法值：数字、字符串等应定义为常量或配置

---

## 二、注释规范

### 要求

- 所有类/接口/组件：简要说明职责与用途
- 所有公共方法/函数：说明功能、参数、返回值、可能异常
- 复杂业务逻辑：分步或代码块注释
- 临时方案：使用 `TODO` / `FIXME` / `NOTE` / `HACK`，并注明负责人或日期（若团队有约定）
- 严格遵循 JSDoc / PyDoc / JavaDoc 等标准格式
- 禁止在代码与注释中使用 emoji 或装饰性符号（见 `code-output-and-integrity` 规则）

### Python 示例（PyDoc）

```python
def validate_token(token: str, request_id: str) -> UserInfo:
    """
    验证用户 JWT 令牌并提取用户信息。

    Args:
        token: JWT 令牌字符串，不能为空
        request_id: 请求追踪 ID，用于日志关联

    Returns:
        UserInfo: 用户信息对象

    Raises:
        TokenExpiredException: 令牌已过期
        ValueError: 参数为空或格式不正确
    """
```

### TypeScript / Vue 示例（JSDoc）

```typescript
/**
 * 验证用户 JWT 令牌并提取用户信息
 * @param token - JWT 令牌字符串
 * @param requestId - 请求追踪 ID
 * @returns 用户信息对象
 */
async function validateToken(token: string, requestId: string): Promise<UserInfo>
```

---

## 三、错误处理与提前返回

### 原则

- 在函数/方法**开始处**处理错误与边界情况（保护子句 / Guard Clauses）
- 对错误条件使用**提前返回**（Early Return），避免深层嵌套
- 正常流程（Happy Path）放在最后
- 避免不必要的 `else`，多用 `if + return`
- 提供清晰的错误信息与适当日志
- 使用自定义错误类型或统一错误码（若项目已约定）

### 示例（避免深层嵌套）

```python
# 不推荐：深层嵌套
def process_order(order):
    if order is not None:
        if order.is_valid():
            if order.has_stock():
                return process(order)
            else:
                return "无库存"
        else:
            return "订单无效"
    return "订单不存在"

# 推荐：提前返回
def process_order(order):
    if order is None:
        return "订单不存在"
    if not order.is_valid():
        return "订单无效"
    if not order.has_stock():
        return "无库存"
    return process(order)
```

---

## 四、代码质量与结构

### 通用原则

- 单一职责：一个函数/类只做一件事
- DRY：不重复逻辑，抽取公共函数/组件
- 方法/函数长度：建议不超过 80 行，复杂业务可适当放宽
- 参数个数：建议不超过 7 个，过多时用对象/配置封装
- 类型：Python 使用 type hints，TS/JS 使用 TypeScript 类型或 JSDoc

### 项目结构参考

- Python（本仓库后端）：`app/` 下按 `api/`、`models/`、`services/`、`core/` 等分层；配置与敏感信息用环境变量
- Vue/TS（本仓库前端）：`src/` 下按 `components/`、`views/`、`api/`、`utils/`、`types/` 等组织；公共逻辑抽到 composables/utils

---

## 五、与本项目技术栈的衔接

- 后端：FastAPI + ClickHouse/Redis，遵循 FastAPI 与 Pydantic 惯例，异步 I/O 用 `async/await`
- 前端：Vue 3 + TypeScript，组件与 API 层分离，类型与 `docs/` 中接口设计保持一致
- 做架构或接口变更时，与 `docs/` 下产品需求、数据设计、接口设计、页面规格同步，并更新相关类型与文档
