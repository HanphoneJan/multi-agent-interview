---
description: 修改代码后必须执行 lint 检查与编译/测试验证，确保通过后再声称完成。
alwaysApply: true
---

# 代码修改后的验收验证

每次修改完代码后，必须完成以下验收流程，通过后再声称任务完成或建议用户提交。

## 一、Lint 检查

### 前端（code/web）

```bash
cd code/web && pnpm run lint
```

- 执行 ESLint，带 --fix --cache
- 若有报错，须先修复再继续

### 后端（code/server，当修改了 Python 文件时）

```bash
cd code/server && uv run ruff check app worker
```

- 若本次仅改动了特定文件，可限定范围：`uv run ruff check app/path/to/file.py`
- 若有报错，须先修复再继续

## 二、编译 / 类型检查

### 前端（修改了 Vue/TS 时）

```bash
cd code/web && pnpm run build-only
```

- 或 `pnpm run build`（含 type-check）
- 必须通过，无编译错误

### 后端（修改了 Python 时）

- 无单独「编译」步骤；类型检查可依赖 ruff、mypy（若项目配置）
- 至少保证 ruff 通过

## 三、测试验证

### 方式 A：运行已有测试

**前端单元测试：**

```bash
cd code/web && pnpm run test:unit
```

**后端单元/验收测试：**

```bash
cd code/server && uv run pytest tests/ -v --ignore=tests/acceptance
```

- 或针对改动模块：`uv run pytest tests/test_xxx.py -v`
- 验收场景：`uv run pytest tests/acceptance/phase1/ -v`（不依赖 ClickHouse/Redis）

### 方式 B：编写或补充测试

- 对新增或变更逻辑，应补充/编写对应单元测试或组件测试
- 测试文件位置：`code/web/src/**/__tests__/` 或 `code/server/tests/`
- 编写后执行对应测试命令验证通过

## 四、执行顺序建议

1. 修改代码
2. 执行 lint（修复 lint 问题）
3. 执行编译 / 构建（修复编译错误）
4. 运行相关测试（或编写测试后运行）
5. 全部通过后，再声称完成或建议提交

## 五、例外

- 仅修改文档（.md）、配置（如 .json、.yaml）或纯样式时，可只做与改动相关的验证（如前端仅 build、后端可跳过）
- 若项目无对应测试套件，至少完成 lint + 编译/构建验证
