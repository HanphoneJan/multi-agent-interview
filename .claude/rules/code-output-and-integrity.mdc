---
description: 代码输出规范与功能完整性保护。禁止在代码/注释中使用 emoji，禁止擅自简化或删减功能。
alwaysApply: true
---

# 代码输出与功能完整性

## 一、代码输出规范（最高优先级）

### 严格禁止在代码与注释中使用

- Emoji 表情符号（如 正确 错误 等）
- Unicode 装饰符号（如 等）
- 特殊图形字符（如 等）
- 任何非标准 ASCII 的装饰性符号

### 代码注释必须

- 使用纯文本（中文/英文）
- 使用标准注释符号（`//` `#` `/* */`）
- 清晰表达功能，无需图标装饰

### 示例

- 错误：在注释中使用 emoji 或装饰符号（如 `// 用户认证成功 [带符号]`）
- 正确：`// 用户认证成功`（纯文本）
- 错误：`# 参数验证失败 [带符号]`
- 正确：`# 参数验证失败`（纯文本）

### 原因

- 在部分编辑器/终端中显示异常
- 影响代码审查和版本控制
- 不符合专业编码规范
- 破坏跨平台兼容性

---

## 二、代码完整性保护

### 严格禁止

- 以任何理由删减已实现的功能代码
- 移除已实现的功能特性
- 以「优化」名义删除看似冗余的代码
- 未经确认合并或简化功能
- 简化复杂业务逻辑
- 为「代码整洁」而删除功能代码

### 保护原则

- 功能完整性高于一切
- 宁可冗余，不可缺失
- 保留所有边界处理与错误处理
- 保持原有复杂度，尊重历史代码意图

### 修改代码时

- 若将删除代码行，先确认是否影响功能
- 若修改可能简化功能，需明确说明并确认
- 修改前可声明：本次修改保证不简化功能、不删减特性、不合并逻辑

---

## 三、与本项目的关系

- 本规则与 `dev-principles` 中的 KISS/YAGNI 并存：在**不删减既有功能**的前提下追求简洁与必要。
- 重构或「优化」时：只做增强与整理，不擅自移除能力或合并入口，有疑问时先确认再改。
